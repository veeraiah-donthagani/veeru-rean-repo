#!/bin/sh

#
# This file was generated by Chef.
# Do NOT modify this file by hand.
#

[ -r /etc/profile ] && . /etc/profile

<% groups = ([node['REAN-jenkins']['master']['user']] + node['REAN-jenkins']['master']['runit']['groups']).join(':') %>
exec 2>&1
cd <%= node['REAN-jenkins']['master']['home'] %>
touch jenkins.start
<% limits = node['REAN-jenkins']['master']['ulimits'] %>
<% if limits && !limits.empty? %>
ulimit <% limits.each { |option, value| %>-<%= option %> <%= value %> <% } %>
<% end %>
exec chpst -u <%= groups %> -U <%= groups %> \
  env HOME=<%= node['REAN-jenkins']['master']['home'] %> \
  JENKINS_HOME=<%= node['REAN-jenkins']['master']['home'] %> \
  <%= node['REAN-jenkins']['java'] %> <%= node['REAN-jenkins']['master']['jvm_options'] %> -jar jenkins.war \
  --httpPort=<%= node['REAN-jenkins']['master']['port'] %> \
  --httpListenAddress=<%= node['REAN-jenkins']['master']['listen_address'] %> \
  <%= node['REAN-jenkins']['master']['jenkins_args'] %>
