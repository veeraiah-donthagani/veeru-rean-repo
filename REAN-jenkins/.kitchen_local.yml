---
driver:
  name: vagrant
  network:
    - ["forwarded_port", {guest: 8080, host: 8080}]

provisioner:
  name: chef_solo

platforms:
  - name: centos/7
  - name: ubuntu-16.04

suites:
  - name: default
    run_list:
     - recipe[REAN-jenkins::default]
    attributes:
